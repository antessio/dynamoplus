name: Docker Image CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: docker login
      env:
        DOCKER_USER: ${{secrets.DOCKER_HUB_SECRET}}
        DOCKER_PASSWORD: ${{secrets.DOCKER_HUB_USERNAME}}
      run: |
        docker login -u $DOCKER_USER -p $DOCKER_PASSWORD 
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag dynamoplus-${GITHUB_REF##*/}
    - name: Docker Push
      run: docker push dynamoplus-${GITHUB_REF##*/}
